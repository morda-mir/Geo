# morda custom geo

Small custom `geosite.dat` and `geoip.dat` for Xray / RemnaWave / Happ.

## Categories

- `ext:geosite.dat:MORDA-PROXY`
- `ext:geosite.dat:MORDA-ADS`
- `ext:geoip.dat:TELEGRAM-IP`

## Build

```bash
python3 scripts/build.py
```

Artifacts appear in `dist/`.
